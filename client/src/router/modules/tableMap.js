/* import Layout from '@/views/layout/Layout'
import constants from '@/constants/index'

const tableMapRouters = {
  path: '/table-map',
  component: Layout,
  redirect: 'table-map',
  children: [
    {
      path: '',
      component: () => import('@/views/table_map/index'),
      name: 'Table Map',
      meta: {
        title: 'Mapa de mesas',
        icon: 'pending',
        roles: [constants.roles.ADMIN]
      }
    }
  ]
}

export default tableMapRouters */
